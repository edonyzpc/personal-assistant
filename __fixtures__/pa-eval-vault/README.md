# PA Eval Synthetic Vault

This vault contains synthetic notes for deterministic PA eval fixtures.

Do not place real user notes, provider output, credentials, or mutable smoke
artifacts here. The existing `test/` vault is for Obsidian smoke testing; this
directory is for stable repo-local eval cases.
