interface:
  display_name: "PA SDD Lifecycle"
  short_description: "按复杂度交付 PA 计划、设计与实现"
  default_prompt: "Use $sdd-lifecycle to deliver approved PA scope with a minimal README plus Tracker package, adding Plan or SDD only when complexity requires them, and never inferring closeout or Git authority."
policy:
  allow_implicit_invocation: false
