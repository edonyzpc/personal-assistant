# Independent Review — OPT-004

## Round 1

Reviewer: independent read-only subagent `review_opt004`  
Conclusion: CHANGES_REQUIRED

### OPT-004-R1

ID: OPT-004-R1  
严重程度: P1  
状态: CONFIRMED  
文件与位置: `src/pa/saved-insight-store.ts` record validation  
问题机制: only `sourceRefs` were checked for forbidden raw/provider text, while `cloneInsight()` preserved top-level extra keys.  
触发条件: otherwise-valid persisted Insight contains `fullProviderOutput`, `rawText`, or a related forbidden top-level key.  
影响: sensitive raw text survives normalization and may be persisted again.  
证据: independent bundle probe retained top-level `fullProviderOutput`.  
建议方向: scan the complete record and retain valid sibling filtering.  
验证方式: malicious-plus-valid mixed normalization probe.

### OPT-004-R2

ID: OPT-004-R2  
严重程度: P2  
状态: CONFIRMED  
文件与位置: `src/pa/contracts/source-ref.ts` absolute path validation  
问题机制: leading backslash/rooted and UNC Windows paths were not rejected.  
触发条件: `\private\note.md` or `\\server\share.md`.  
影响: non-vault-relative source refs pass all ledger normalizers.  
证据: independent direct probes returned `{ok:true}`.  
建议方向: treat a leading backslash as absolute and add rooted/UNC regressions.  
验证方式: direct validator assertions.

Primary-agent classification: both 成立.

Resolution:

- Saved Insight now scans the entire persisted record for forbidden text keys.
- SourceRef rejects both slash and backslash roots.
- Both regressions failed before correction and pass afterward.
- Three focused suites 27/27 plus lint, type-check, and build pass.

## Round 2

Reviewer: a new read-only review turn after the correction  
Conclusion: ACCEPT

未发现阻止该任务通过的实质性问题。

- Independent probes confirm top-level forbidden Insight text is dropped while a valid sibling remains.
- Rooted and UNC backslash paths now return `absolute_path`; valid Vault-relative paths still pass.
- Malformed source refs, scopes, string arrays, summary/text fields are rejected before clone helpers.
- No new rejection of a value inside the declared persisted type contracts was found.

Reviewer verification: three focused suites 27/27, type-check, and independent bundle probes pass. Full regression, lint/build repetition, and app smoke remain global gates.
