# Independent Review — OPT-002

## Round 1

Reviewer: independent read-only subagent `review_opt002`  
Conclusion: CHANGES_REQUIRED

### OPT-002-R1

ID: OPT-002-R1  
严重程度: P2  
状态: CONFIRMED  
文件与位置: `src/plugin.ts` Pagelet teardown and Quick Capture factory; focused lifecycle test  
问题机制: the first implementation cleared `quickCaptureService` in `destroyPageletRuntime()`. A Pagelet settings sync could therefore split two open/capturing modals across distinct append queues.  
触发条件: first capture remains in flight, Pagelet runtime is torn down, then a second modal saves to the same note.  
影响: the original read-modify-write loss window remained reachable.  
证据: factory identity only held before teardown; `QuickCaptureService` queues are instance-local.  
建议方向: preserve the service across Pagelet teardown and clear it only during plugin unload.  
验证方式: add a teardown-interposed identity regression and rerun Quick Capture suites.

Primary-agent classification: 成立.

Resolution:

- Added a regression that failed under the reviewed implementation.
- Removed the reset from `destroyPageletRuntime()`.
- Added the reset to `unloadAsync()` only.
- Focused post-fix result: 3 suites, 72/72 tests pass; lint, type-check, build, and whitespace pass.

## Round 2

Reviewer: new independent read-only subagent `review_opt002_r2`  
Conclusion: ACCEPT

未发现阻止该任务通过的实质性问题。

- The service now survives Pagelet teardown/settings sync and is reset only on PluginManager unload.
- Cache ownership is per PluginManager, so no cross-Vault sharing occurs.
- Runtime settings mutate the existing settings object; the service reads settings on every capture, so no stale settings path was found.
- The identity regressions plus the existing same-service concurrent append test jointly cover the mechanism.
- No Quick Capture format, target path, public API, or unrelated behavior changed.

Reviewer command: `npm test -- --runInBand __tests__/plugin-record-note.test.ts __tests__/quick-capture.test.ts` — PASS, 2 suites and 67/67 tests.
