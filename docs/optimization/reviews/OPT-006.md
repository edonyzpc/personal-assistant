# Independent review — OPT-006

## Round 1

Reviewer: independent read-only agent; not involved in implementation.

### OPT006-R1

- Severity: P1
- Status: CONFIRMED
- Location: `src/pa/contracts/memory-taxonomy.ts`; `src/plugin.ts`
- Mechanism: automatic eligibility checked Memory type but not sensitivity, so
  a valid `medium` candidate could enter durable Memory without confirmation.
- Evidence: provider parsing admits `medium`; validation rejects only `high`;
  `canAutoConfirmMemoryCandidate()` originally accepted only a type field.
- Required direction: permit automatic confirmation only for `low` sensitivity
  and add a Level 2 medium regression.

### OPT006-R2

- Severity: P2
- Status: CONFIRMED
- Location: `src/settings.ts`
- Mechanism: the pause toggle mutated runtime settings before persistence; a
  rejected save left the UI looking paused while restart restored auto Memory.
- Required direction: transactional UI/runtime rollback with localized failure
  feedback, followed by a durable retry test.

### OPT006-R3

- Severity: P2
- Status: CONFIRMED
- Location: `src/plugin.ts`
- Mechanism: canonical removal succeeded before queue audit update. A second
  write failure or disabled Review Queue left the exact linked item permanently
  `applied`, with no retry path.
- Required direction: retain the tombstone as source of truth and durable retry
  marker; reconcile exact `applied -> undone` links on startup/settings changes
  without the user-facing enabled gate.

### OPT006-R4

- Severity: P2
- Status: CONFIRMED
- Location: `src/pagelet/tab/PageletDetailView.ts`;
  `src/pagelet/tab/sections/MemoryGovernanceSection.ts`
- Mechanism: Remove updated only Tab action state; the native detail session
  cache retained active text/source payload and could restore it in-session.
- Required direction: return the actual tombstone and update the cached detail
  payload before native state restoration.

Round 1 conclusion: **CHANGES_REQUIRED**.

## Main-agent disposition

- R1: accepted and fixed; eligibility now requires `low`, with contract and
  production-pipeline regressions.
- R2: accepted and fixed; failed persistence restores the previous runtime/UI
  value and shows localized failure feedback; successful retry reloads paused.
- R3: accepted and fixed; exact linked tombstones are idempotent retry markers,
  reconciled immediately, on idle startup, and on relevant settings changes.
  Reconciliation bypasses only the display/producer enabled gate.
- R4: accepted and fixed; Pagelet detail caches the returned text-free tombstone
  and a same-session restore regression proves no summary/source resurrection.

Post-fix focused validation: 4 suites / 242 tests, TypeScript, lint, and
`git diff --check` passed.

## Round 2

Reviewer: a second independent read-only agent; not involved in implementation
or round 1.

### OPT006-R2-01

- Severity: P2
- Status: CONFIRMED
- Location: `src/plugin.ts`; `src/pa/review-queue-store.ts`
- Mechanism: confirmation could persist the canonical record but fail the
  subsequent queue `accepted -> applied` write. Removal then saw `accepted`,
  while the first reconciler handled only `applied`, leaving a durable hidden
  audit fork.
- Required direction: for an exact linked tombstone, reconcile
  `accepted -> applied -> undone`; retain the tombstone for retry if either
  write fails.

Round 2 conclusion: **CHANGES_REQUIRED**.

Main-agent disposition: accepted and fixed. The reconciler now advances exact
linked `accepted` audit state through the existing valid transition before
marking it `undone`. A failure-injection regression independently fails the
confirmation apply write and the immediate removal reconciliation write, then
proves a disabled-queue reload converges and remains idempotent. Latest focused
result: 5 suites / 253 tests; TypeScript, lint, and diff check pass.

## Round 3

Reviewer: a third independent read-only agent; not involved in implementation
or prior reviews.

### OPT006-R3-01

- Severity: P1
- Status: CONFIRMED
- Location: `src/plugin.ts`; `src/settings.ts`;
  `src/quick-capture-enrichment.ts`
- Mechanism: `memoryEnabled=false` hid all Memory sub-controls and stopped
  MemoryManager, but the Quick Capture candidate path still checked only
  trust/pause and could create an automatic durable record.
- Required direction: the Memory master setting must be the first automatic
  eligibility gate; add a production creation-chain regression.

Main-agent disposition: accepted and fixed. `shouldAutoConfirmMemoryCandidates()`
now requires `memoryEnabled === true`; the new Level 2 master-off regression
keeps the item suggested, creates no record, and leaves the trust count intact.
Latest focused result: 5 suites / 254 tests; TypeScript, lint and diff check
pass.

The reviewer rechecked the final code and found no additional substantive
issue. Independent focused validation passed 5 suites / 257 tests, TypeScript,
lint, diff check, and the community DOM source scan.

Round 3 conclusion: **ACCEPT**.

## Global-integration re-reviews

Fresh read-only reviewers independently accepted the post-verification fixes:

- active exact-linked records now retry `accepted -> applied` using real Store
  persistence/reload evidence;
- malformed `confirmedMemoryCount` values fail closed at load and every runtime
  trust gate;
- pause read/apply/save/rollback is contained in the shared settings queue and
  a failed value cannot hitchhike on a sibling ledger write;
- concurrent writers that already reached the exact target audit state are
  treated as idempotent, while thrown errors and non-target states still log.

The final idempotency reviewer ran 68/68 focused tests, TypeScript, and diff
check and concluded **ACCEPT**, with no new P0/P1/P2.
