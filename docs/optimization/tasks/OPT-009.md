# OPT-009 — Record Quiet Recall Link acceptance once

## Pre-implementation revalidation

- Original problem: successful Bubble Link calls the shared PluginManager Link
  host, which already records one accept signal, then the orchestrator records a
  second accept for the same user action.
- Root cause: feedback ownership exists in both the shared Link host and one UI
  caller. Existing tests used a side-effect-free host mock and hid production
  wiring.
- Modification boundary: production-style orchestrator regression and deletion
  of the duplicate orchestrator accept call.
- Non-goals: no historical profile migration, signal-shape/ranking change,
  frontmatter/link behavior change, or feedback changes for view/dismiss/later.
- Acceptance: successful Link produces exactly one accept signal; failed or
  stale-active-note paths produce none; all Link entry points retain the shared
  PluginManager owner.

## Implementation and initial validation

- The production-style host test failed with two accept calls before the fix.
- Removed only the orchestrator's successful-Link accept call; the shared
  PluginManager Link host remains the owner for every entry point.
- `pagelet-orchestrator.test.ts` now passes 43/43; the combined module bundle,
  lint, type-check, build and whitespace pass.
- Historical double-counted aggregates are not heuristically migrated.

## Independent gates

- Review: ACCEPT with one non-blocking P3 test-depth residual: the orchestrator
  test simulates the private PluginManager owner; production tracing and
  frontmatter tests complete current evidence.
- Verification: PASS. Independent baseline/current tracing, 6 suites / 162
  tests, TypeScript, lint, build and diff check passed.

## Rollback

- Restore only the removed successful-Link accept call in
  `src/pagelet/orchestrator.ts` and revert the matching production-style test
  expectation in `__tests__/pagelet-orchestrator.test.ts`.
- No persisted Retrieval Habit Profile migration occurred, so historical
  aggregates are neither rewritten nor deleted during rollback.
- Re-run the orchestrator/profile/frontmatter focused suites, type-check, lint,
  build, and diff check.

Status: review ACCEPT, independent verification PASS, and final 137-suite
regression PASS. The disclosed P3 signal-shape/test-depth residuals remain
outside this duplicate-counting task.
