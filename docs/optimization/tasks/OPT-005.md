# OPT-005 — Remove mobile Debug global Console capture

## Pre-implementation revalidation

- Original problem: mobile Debug startup invokes a helper that replaces five process-wide Console methods, captures other plugins, stores unredacted strings in an unbounded array/file, and never restores globals.
- Independent baseline mock reproduced persistent patching, cross-plugin/sensitive text in `logs.txt`, and full-history rewrites.
- Root cause: a legacy global debugging helper bypasses the plugin's existing scoped `log()` and `redactForLog()` path.
- Modification boundary: remove the only production import/call and now-unreferenced helper; remove two obsolete Jest mocks; add a scoped logger redaction regression/source scans.
- Non-goals: no new file logger, redaction-policy expansion, settings change, or non-Debug behavior change.
- Acceptance:
  1. Production source contains no global Console assignment or helper invocation.
  2. Mobile Debug continues to use PluginManager's existing scoped console logger.
  3. Plugin logger redacts secret-shaped strings and sensitive object keys.
  4. Build, community scan, and focused plugin tests pass.

## Implementation and verification log

Status: completed.

- Plan versus actual: matched. Removed the only production wiring and deleted the unreferenced global helper; cleaned two obsolete Jest mocks; added scoped redaction coverage.
- Modified files: `src/plugin.ts`, deleted `src/obsidian-hack/obsidian-mobile-debug.ts`, `__tests__/plugin-record-note.test.ts`, `__tests__/get-vss-files.test.ts`.
- Behavior change: mobile Debug no longer captures other plugins or persists unbounded `logs.txt`; plugin-owned Debug Console logging and startup Notice remain.
- Baseline evidence: independent mock and fixed-commit source show five global assignments, unbounded buffer, unredacted cross-plugin content, and no disposer.
- Post-fix checks: focused 64/64; lint, type-check, build, bundle audit, whitespace and source/bundle scans pass.
- Independent review: ACCEPT, no findings.
- Independent verification: PASS.
- Later global gate: desktop test-vault smoke passed. Real mobile/iOS Debug startup remains unexecuted; source and bundle absence directly cover the removed patch mechanism. See `../verification/FULL-REGRESSION.md`.
- Remaining task-local risk: no known global patch path; redaction pattern expansion remains out of scope.
- Rollback: restore the deleted helper and import/call/mocks/test hunks; no user data migration is involved (existing historical `logs.txt`, if any, is deliberately not deleted).
