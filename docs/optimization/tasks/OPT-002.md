# OPT-002 — Share Quick Capture append coordination across modals

## Pre-implementation revalidation

- Original problem: each command creates a fresh `QuickCaptureService`, while the same-path append queue is owned by one service instance.
- Root cause: two modals therefore bypass each other's read-modify-write serialization.
- Modification boundary: one PluginManager cache field/factory/unload reset plus a focused identity regression; no Quick Capture format or UI change.
- Non-goals: no static/global queue, no modal suppression, no destination or post-processing change.
- Acceptance:
  1. Repeated plugin service requests return the same instance.
  2. Existing same-instance concurrent append test continues to preserve both entries.
  3. Service reference is released on plugin unload.

## Implementation and verification log

Status: completed.

- Plan versus actual: the singleton approach remained; an independent review found the initial cleanup point was too broad, so cleanup moved from Pagelet teardown to plugin unload.
- Modified files: `src/plugin.ts`, `__tests__/plugin-record-note.test.ts`.
- Behavior change: all Quick Capture modals in one plugin lifetime share one per-path append queue, including across Pagelet runtime teardown.
- Tests added: factory identity and Pagelet-teardown identity. Existing concurrent append test proves both same-path entries survive.
- Pre-fix evidence: identity test failed with distinct services; first review regression also failed before the cleanup-point correction.
- Post-fix checks: related 72/72; lint, type-check, build, whitespace pass.
- Independent review: round 1 CHANGES_REQUIRED (P2,成立), corrected; round 2 ACCEPT.
- Independent verification: PASS.
- Later global gate: two empty Quick Capture modals mounted concurrently and closed without writes; concurrent append correctness remains covered by the automated queue test. See `../verification/FULL-REGRESSION.md`.
- Remaining task-local risk: no known correctness issue.
- Rollback: revert the cache field/factory/unload hunks and lifecycle tests; no persisted data changes.
