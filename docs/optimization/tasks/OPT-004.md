# OPT-004 — Fail closed on malformed persisted PA ledger entries

## Pre-implementation revalidation

- Original problem: startup settings normalization casts untrusted persisted objects to TypeScript interfaces, then calls `.trim()`, `.length`, `.map()`, or `cloneScope()` before runtime shape checks.
- Baseline bundle reproductions: missing Memory `summary`, missing Insight `text`, and Review Queue `sourceRefs:[null]` each throw a TypeError.
- Root cause: compile-time types are treated as runtime guarantees at the persistence boundary.
- Modification boundary: source-ref and review-queue contract validators, three normalizer/store validators, and their three test files (8 files).
- Non-goals: no repair guesses, migration, valid data rewrite, new persisted fields, or behavior change for well-formed records.
- Acceptance:
  1. A malformed entry never throws during normalize.
  2. Only that entry is discarded; valid siblings remain byte-equivalent at the business-field level.
  3. Declared scope/source-ref/string-array shapes are enforced before clone helpers.
  4. Existing path traversal, raw-text, lifecycle, and admission security checks remain intact.

## Implementation and verification log

Status: completed.

- Plan versus actual: the intended runtime-shape guards were implemented. Round-1 review added two in-boundary corrections: top-level forbidden Saved Insight text and backslash-root/UNC path rejection. File count is eight, at the configured soft limit.
- Modified files: `src/pa/contracts/source-ref.ts`, `src/pa/contracts/review-queue.ts`, three ledger store/normalizer files, and their three tests.
- Behavior change: malformed records are dropped individually before clone; valid siblings remain. Declared runtime shapes, forbidden raw text, traversal, and all absolute path forms fail closed.
- Tests added: mixed malformed states, direct null source ref, bad scope/why arrays, top-level provider output, rooted and UNC paths.
- Pre-fix evidence: three TypeErrors (24 existing tests passed); review regressions separately failed for provider text and rooted path.
- Post-fix checks: direct 27/27; settings/PA 216/216; PA eval 9/9; lint, type-check, build, whitespace pass.
- Independent review: round 1 CHANGES_REQUIRED (P1 + P2, both 成立), corrected; round 2 ACCEPT.
- Independent verification: PASS with baseline and current bundles.
- Later global gate: full repository suite passed; see `../verification/FULL-REGRESSION.md`.
- Remaining task-local risk: none known.
- Rollback: revert the eight OPT-004 hunks together; malformed entries would again be unsafe, but no migration or data rewrite is involved.
