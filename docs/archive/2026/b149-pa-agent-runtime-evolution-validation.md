# B-149 PA Agent Runtime Evolution 验证记录

本页保留 B-149 完成后的逐例验证事实与人工判读，不作为当前运行或发布状态权威；稳定行为见 [Product Spec](../../product/specs/pa-agent-runtime-evolution-product-spec.md) 和 [PA Agent Architecture](../../architecture/pa-agent-architecture-plan.md)。13/13 AC 的本地验收记录可从 Git 中的原 Tracker 恢复。测试只使用虚构材料、repo `test` vault 和已配置的 `qwen/deepseek-v4-pro`。模型参数为 `temperature=0.8`、stream；固定 Web 响应用于可重复任务比较，另有一次真实 Web MCP 连接检查。真实模型物理请求累计 **99/180**，其中 G0 基线 23 次，后续 76 次。180 是本次测试总授权（50 + 30 + 100），与 PA 平时每 run 的辅助摘要 30 次/60 分钟上限无关。

最终 `3503e50d…` bundle 经 `make deploy` 的 lint/build/328 suites/8325 tests 自然通过，部署到 test vault 后完成桌面及 CLI mobile simulator 的相关交互验证；最终包 E-05 和自动后台 Pagelet/Chat 并行亦有同包证据。模拟器不证明 iOS 真机键盘或真机 BRAT 安装。开发验收、Git 集成和 beta 发布分别核验。

## 输入和证据口径

- G0 原始报告：`/private/tmp/b149-g0-live-baseline.json`、`/private/tmp/b149-g0-live-baseline-2.json`；bundle SHA-256 `f8cb9b3ece6b1852e9f6a78a8960b90ff92169bf92e2cfdea312f0161286dee3`，fixture SHA-256 `d6e75898474b0cfef17bfb98418e9cde6dff88c6fd10ef434abfb251677d8e71`。G0 的物理 token 归属未知，不能计算相对 token 节省。
- R2 fixture SHA-256 `aec67d51749e2c76863d7509fb65c3a410c614b9551f615ff74403e1c3e6dea9`。E-09 的历史/lineage 相对 G0 改过，不能作完全同输入提升。E-02/03/08 在旧版是新范围能力缺口，没有同范围基线。
- 后续模型报告跨多个 bundle：`75bc2d80…`（E-02/03/08/09），`82fa97b6…`（E-01/04/05），`59b31dcf…`（E-06/07/12，含 E-06 第二次复测），`981f0fe5…`（E-04/05/06 定向复测），`3503e50d…`（新增通用事实边界后的 E-05）。它们不是同一最终输入。一次运行中的 `requestTrace`、`requestBodies`、工具结果、输出和 usage 在各原始 JSON 中；跨 bundle 的结果只能用于对应行为观察，不合成一个“最终版 12/12”。最后一次源码变化只增加回答事实边界指令；E-04/06 的领域投影和工具观察没有改变，按影响面复用较早 bundle 的语义证据。
- 下表时间单位为秒，取**已交付输出**或实际终态的 `deliveredMs`，不是 provider EOF。已完成案例的首次可见输出即最终交付，人工将其作为该例首个有用候选；被取消案例没有可用输出。原始 `firstUsefulMs` 均为 `null`，人工判读不会反写为机器测量。物理 token 只在 attribution 完整时列确数；价格未核实，所有金钱成本未知。
- 表中请求数指物理派发；所列后续案例的请求 purpose 均为 `answer`，没有辅助摘要。E-01/02/03/04/05/07/08/09/12 的 logical/physical 数相同；E-06 首次复测 4 个 logical call 中仅派发 3 次，第二次复测 7 个、最终复测 5 个 logical/physical call 均已派发。每次物理 attempt 的 usage 与完整度见原始报告，不把未派发的 logical call 当费用。

## 逐例结果

| 案例 | G0 基线 | 后续真实模型：请求 / 物理 token / 交付秒 | 人工语义判读与局限 |
| --- | --- | --- | --- |
| E-01 最终决定 | 5 次 / token 未知 / 36.129；找对 UTF-8 CSV | 4 / 26,502 / 24.278 | 从三篇合成笔记找对最终 CSV 和合作方只接收 CSV 的理由；来源路径齐全。后续版 `82fa97b6…`，不是最终冻结版。 |
| E-02 纯网络 | 旧版无该范围 | 2 / 3,295 / 7.140 | 只引固定网页，实际模型请求没有私有哨兵；固定网页不证明在线搜索质量。 |
| E-03 综合比较 | 旧版无该范围 | 8 / 58,135 / 52.515 | 37 单位内部预算与公开两阶段各有所属来源，语义完成，但请求/token/等待显著，不能称为成本改进。 |
| E-04 存在性零命中 | 2 次 / token 未知 / 12.875；错误断言“没记过” | 较早 3 / 15,823 / 27.047；`981f0fe5…` 3 / 15,715 / 24.640 | 最终定向复测的 Vault 零命中结论限于本次搜索，承认 Memory 不可用并说明无法确认曾否记录；**该例语义符合**。范围为零篇许可笔记，不证明一般非空库情况；较早包措辞过宽的记录保留。 |
| E-05 缺材料总结 | 3 次 / token 未知 / 24.767；说明缺材料 | 较早 3 / 14,148 / 19.132；`981f0fe5…` 3 / 17,526 / 16.420；`3503e50d…` **3 / 15,443 / 19.839** | Owner 明确接受 `981f0fe5…` 基于当前线索作“很可能不存在”的不确定推断：它已说明 Memory 不可用、没有编造最终决定，且未把总结当作完成，故更正此前“仅部分符合”为**该例语义符合**。`3503e50d…` 更明确区分空结果与不可用并说总结未完成，也符合；单次对照不证明新增提示带来质量或成本收益。许可范围扫描为零篇笔记，不推广为非空 vault 召回质量。 |
| E-06 暂不可用 | 5 次后请求上限取消 | 首次 3 / 17,202 / 15.898，批次取消；第二次 7 / 43,498 / 45.842，语义失败；`981f0fe5…` 5 / 31,769 / 39.261 | 第二次 Vault 的 11 个标准只读失败被 `result_unknown` 掩盖，模型误称工具未执行；最终包 6 个标准失败可见，模型明确查找失败、不能据此推断笔记不存在，**该例语义符合**。另 3 个来源读取计划未准入结果保守 unknown，未污染结论。三批物理请求均计入总额。 |
| E-07 冲突版本 | 4 次 / token 未知 / 31.489；正确 30/17 | 4 / 26,889 / 24.350 | `59b31dcf…` 报告先经历多种检索词零命中，继而读到 v1/v2 两路径，答对旧 30 天、最终 17 天及来源；这说明换线索后的合法查找不能用固定零命中次数禁止。 |
| E-08 跨范围追问 | 旧版无该范围 | 1 / 2,131 / 18.627 | 没有把旧私有预算送出；由于指代条件不足，先澄清，未做 Web 检索。证明安全澄清，不证明 Web 追问最终答案。 |
| E-09 动作轨迹 | 3 次 / token 未知 / 24.526；丢 draft/final 参数区别 | 1 / 5,584 / 4.000 | 正确区分两次同结果查询的 `draft`/`final` 参数；R2 fixture 与 G0 不同，不能量化同输入增益。 |
| E-10 恢复/副作用 | 离线旧版可停错 | 无真实模型请求 | 固定 HTTP 8 次脚本覆盖新证据后继续、无进展收束；未知副作用分支仍是离线覆盖限制，不作真实任务质量声称。 |
| E-11 取消等待 | 离线旧版 lease 卡住 | 无真实模型请求 | 可控挂起准备、取消和后续 Chat lease 在已部署 app 中通过；这是确定性生命周期证据，不以 provider 费用复测。 |
| E-12 显式 Writing | 1 次后请求上限取消 | 3 / **≥12,266** / 26.277 | 最终 `59b31dcf…` 生成并接纳 671 字公告作品，未声称已保存；独立 App seam 验证保存回执/版本续写。第三个 HTTP response 无 usage，完整 token 与金额未知。虚构草稿的自拟日期和安排不当作笔记事实。 |

R2 固定 HTTP 离线层 12 例、30 次假模型请求和 3 次固定 Web，机械断言没有失败；它证明实际 runtime/SDK 边界与事件传播，不给上表真实语义背书。真实 Web MCP 仅做了一次连接 smoke，返回 1 条 source；底层物理 POST 数和费用未观测。长上下文 App seam 以 24 条历史、3,000 字符预算观察首次 2 次辅助摘要、同输入再次 0 次摘要与正确离线约束；假 usage/耗时不是用户账单或真实模型延迟。最终包另有自动后台 Pagelet 与 Chat 同时运行的离线应用证据：1 次 Pagelet 假模型请求挂起时，Chat 的 1 次假请求及交付先完成，Pagelet 释放后 `quiet/no-insight`；合成笔记未改变、没有其他 Markdown 读取或写入，后台设置与 HTTP 桩已恢复。

## 对照结论和证据限制

- E-01、E-07 保持来源正确；E-02/03 的新范围行为有真实模型例证；E-08 安全澄清；E-09 参数关系可见。定向复测中 E-04 与 E-06 分别给出有限零命中和查找失败结论；E-05 的 `981f0fe5…` 与 `3503e50d…` 均说明缺少材料且未完成总结。合格条件是区分已观察事实、带不确定性的推断与任务完成，不要求禁用“很可能”措辞。各例按原始 bundle 和许可来源判断，不把跨版逐例通过写成同包 12 例完整复跑。
- 完整物理 token 只覆盖有 provider usage 的后续请求；G0 不能计算 token 差额，E-12 只能给下界；价格全未知。多次请求 cap 取消或跨 bundle/fixture 的数据，不拿来声称成本或质量收益。
- E-06 失败的第二次报告在 `test/B149-runtime-eval/g4-e06-extended-report.json`（SHA-256 `304e2b1c21341d6bd26b5e1e995bb3df343f8bfc16e0be46f9356bce0f97f13a`）；后续成功复测在 `test/B149-runtime-eval/g4-e06-final-report.json`（SHA-256 `fbf4632d137b8d74f4f886eee257db1c6a55c9b2668fe82a297a3ec6ebfd9081`）。E-04/05 旧包报告在 `test/B149-runtime-eval/g4-e04-e05-final-report.json`（SHA-256 `2f25fdf0372147378141cf9b82c702f9bfc8f3125c2425d22b149265c873b86a`）；最终 E-05 在 `test/B149-runtime-eval/g4-e05-prompt-final-report.json`（SHA-256 `88b2858fb15db950c8526153ff6fa66aea22b9bdffc3466e727c3edac2bc6692`）。APP-08 自动后台并行的无内容报告在 `test/B149-runtime-eval/g4-app08-background-report.json`（SHA-256 `da213a9dd3b3ebec0220137d12823356a01efc5822883cae5034a6a42d7e6af1`）。旧报告保留为对应 bundle 的历史，不冒充最终包重复验收。
