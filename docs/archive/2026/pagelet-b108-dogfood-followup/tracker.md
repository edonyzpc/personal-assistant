# Pagelet B-108 Dogfood Follow-up Development Tracker

Document status: Archived
Delivery status: Closed
Updated: 2026-07-19
Work item: B-108
Authority: 本 track 的唯一执行状态、finding lifecycle、验证证据与 closeout readiness。
Product spec: [Scope Recap And Theme Summary Product Spec](../../../product/specs/pa-scope-recap-theme-summary-product-spec.md)
Plan: [Delivery Plan](./plan.md)
SDD: [Approved Software Design](./sdd.md)
Closeout: [Final Closeout](./closeout.md)

## Current Snapshot

- Current phase: B-108 implementation、completion audit/fix、自动化与 local/iCloud deploy、桌面/iPhone 物理交互、真实 Qwen semantics/cost、用户主导的正式 3-Second Value Test、`2.9.0-beta.2` prerelease 发布及桌面/iPhone BRAT 安装烟测均完成。首次 CTA-only 样本保留为通用 `intentionally-quiet` 产品反馈，但不计 prepared Recap 验收；正确前置条件下的第二次测试已通过。
- Next action: B-108 已 Closed/Archived；stable release 仍需独立授权，后续真实 dogfood 只按 B-116 的启动条件另开工作。
- Blocker / decision needed: 无验证 blocker，无新的 B-108 产品决策。普通 Pet CTA-only 状态是否也应提供本地价值方向属于未来议题，不阻塞本 track。
- Last verified behavior: 当前树通过 15 focused suites / 807 tests、Scope compatibility focused gate 4 suites / 348 tests、Scope adapter suite 192 tests、最终授权/设置 gate 4 suites / 323 tests，以及 160 full suites / 3165 tests；lint、TypeScript、build、docs、whitespace、community DOM scan、local/iCloud deploy 均通过。用户在正式 disclosure 中亲自选择 `Run` 后，后台以 12 个 bounded sources 成功生成 fresh artifact（995 input + 639 output），Pet 在点击前显示 `insights ready`；点击没有重复 provider call，Recap 对测试 vault 局限的诚实判断建立了信任。发布 gate 与 GitHub Actions run `29685255023` 通过；GitHub prerelease 资产随后通过桌面和 iPhone BRAT 固定 `2.9.0-beta.2` 安装。两端 `main.js` / `styles.css` 与 release asset 字节一致，manifest 语义一致；桌面 Settings、Chat mount、prepared Recap 与 iPhone 44×44 Pet/实体短点 Bubble 路径通过，fresh errors 均为空。

## Work

| ID | Requirement / AC | Slice | Status | Evidence |
| --- | --- | --- | --- | --- |
| T-00 | Authority bootstrap | Create one owning spec, Approved plan/SDD, tracker and registry/backlog transition | [x] | [Feature Home](./README.md), [Plan](./plan.md), [SDD](./sdd.md) |
| T-01 | B-108/REQ-01, B-108/REQ-02, B-108/REQ-06 / B-108/AC-01, B-108/AC-05, B-108/AC-06 | Versioned Run/Adjust/Cancel authorization, independent Recap settings and migration | [x] | Settings/context tests and 5 direct disclosure-action tests pass; real Adjust/Cancel app smoke proves immediate disabled UI, pending/declined persistence, generic-setting independence and zero provider/write |
| T-02 | B-108/REQ-03, B-108/REQ-05, B-108/REQ-07 / B-108/AC-04 | Recap scheduler gates, independent 2/h+10/day bucket, inspectable diagnostics/cache clear | [x] | Complete-snapshot dedupe, provider-boundary revalidation, cross-hot-reload serialization, exact production 2/h and 10/day N/N+1 boundaries, persisted diagnostics, fail-closed storage, status and cache-clear tests pass |
| T-03 | B-108/REQ-04, B-108/REQ-12 / B-108/AC-02, B-108/AC-09 | Actual prepared insight reaches Bubble/Tab immediately with no duplicate call | [x] | Builder/mapper/orchestrator/Tab tests pass; deterministic in-app fake-provider run produced one ready artifact from one call |
| T-04 | B-108/REQ-08, B-108/REQ-09, B-108/REQ-10, B-108/REQ-11 / B-108/AC-07, B-108/AC-08 | High-value Recap gate, stable fingerprint, suppression and independent hint toggle | [x] | Direct background-ready integration proves exactly one nudge; duplicate fingerprint, hint-off, Focus Mode and quiet-hours remain silent; concrete/generic quality regressions also pass |
| T-05 | B-108/REQ-13, B-108/REQ-14 / B-108/AC-03, B-108/AC-10, B-108/AC-11 | Typed failure outcomes and last-valid/attempt/currentness separation | [x] | Failure matrix, source/authorization currentness, late-result and detail-cache invalidation tests pass |
| T-06 | B-108/REQ-15, B-108/REQ-16, B-108/REQ-17 / B-108/AC-12, B-108/AC-13, B-108/AC-14 | Recap Needs Retry, synchronous local overview, Retry/View sources | [x] | Full-folder coverage after boundary filtering, recent-change/time facts, explanation/Tab/source action/retry tests pass; provider-unconfigured Retry opens settings with zero provider/detail duplication/write; attempt diagnostics retain scope kind only |
| T-07 | B-108/REQ-18 / B-108/AC-15 | Independent max-5 Recall evaluation, one language retry, max-10 round | [x] | Coordinator order, sibling isolation, language retry and hard call-count tests pass |
| T-08 | B-108/REQ-19 / B-108/AC-16 | Independent persisted 10/h+50/day Recall actual-call limiter and pre-call reservation | [x] | Exact production 10/h and 50/day N/N+1 boundaries, retry/failure, persisted storage, bucket isolation and fail-closed tests pass |
| T-09 | B-108/REQ-20 / B-108/AC-17 | Exact context-candidate cache, in-flight dedupe and invalidation | [x] | Exact-cache/in-flight tests plus note, source, Saved Insight, provider/model/locale/Data Boundary/policy invalidation pass |
| T-10 | B-108/REQ-21 / B-108/AC-18 | Remove template nudge fallback; local candidates remain explicit Discover-only | [x] | Production adapter and Bubble/Tab/proactive tests keep local-only matches in a localized `Local related clue` source-list presentation with no AI why-now or Recall stack; current-tree real Obsidian fixture confirms the visible separation |
| T-11 | B-108/REQ-22 / B-108/AC-19 | AI provenance gate, default one-card Bubble, qualified stack and diagnostics | [x] | Only `ai` + evaluation fingerprint may enter Recall; accepted IDs win dedupe and cannot re-enter local fallback; mixed accepted/rejected regressions plus single-card/stack, diagnostics, cost attribution and no-queue tests pass |
| T-12 | B-108/AC-02, B-108/AC-07, B-108/AC-09, B-108/AC-12, B-108/AC-19 | Reconcile v2.9 context action, long-press, font and card presentation with current contracts | [x] | Context action coverage, direct production PetView Capture/Review/Discover callback routing, cross-leaf timer/menu teardown, desktop renderer/typography/theme/hover/523ms menu, iPhone 15 short-click/44×44/Bubble/synthetic menu, provider-free real Obsidian Review/Discover route-and-render evidence, and user-reported desktop/iPhone physical long-press pass |
| T-13 | All B-108 requirements and ACs | Focused tests, independent review lanes, local validation gate, `make deploy`, Obsidian formal smoke | [x] | Completion-audit findings fixed; current-tree deploy/tests, deterministic core runtime smoke, authorization/settings real-UI smoke, local-clue and Review/Discover real-renderer fixtures, real Qwen semantics, desktop/iPhone evidence and physical long-press pass. Correctly prepared Recap opened with zero user wait and passed the user-owned 3-Second Value Test |
| T-14 | B-108 delivery | Publish and verify `2.9.0-beta.2` through the BRAT prerelease channel | [x] | Local release gate and GitHub Actions passed; prerelease assets verified; desktop and iPhone BRAT installs are pinned to beta.2 and load runtime version `2.9.0-beta.2` |

Status markers: `[ ] Todo`, `[~] In progress`, `[x] Done`, `[-] Deferred/Cancelled`。

## Findings

| ID | Severity | Finding | Decision / fix | Verification | State |
| --- | --- | --- | --- | --- | --- |
| F-01 | P1 | Baseline prepared Recap scheduling could call without Recap-specific authorization/budget and note activity could cause repeated attempts. | Gate before source transfer/invocation; distinct consent/setting/limiter and snapshot dedupe. | T-01/T-02 provider spies, fake timers and limiter tests. | Resolved |
| F-02 | P1 | Baseline provider failure/generic summary could appear as fresh Delivery and overwrite useful prepared state. | Typed outcomes; concrete-quality mapper; last-valid/attempt/local overview separation. | T-03/T-05/T-06 failure and race matrix. | Resolved |
| F-03 | P1 | Baseline Quiet Recall cooldown path could return local template why-now to the proactive selector. | AI provenance gate; no-template fallback; local candidates Discover-only. | T-07 through T-11 production-wiring tests. | Resolved |
| F-04 | P1 | Existing rate/cost tracking did not prove per-actual-call reservation or independent Recap/Recall attribution. | Feature-scoped persisted buckets and attempt diagnostics. | T-02/T-08 N/N+1, retry, failure, reload and storage-error tests. | Resolved |
| F-05 | P2 | Structured Recap detail and Recap Needs Retry were not proven visible in Tab/Bubble; generic ready copy fails the three-second value bar. | Render actual insight or synchronous local orientation with source actions. | Earlier deployed-build deterministic smoke: ready Detail opened in 5 ms with concrete why/source refs and zero duplicate calls; fallback opened in 6 ms with coverage, Retry and View sources and zero calls. Current-tree automated regressions cover subsequent changes. | Resolved |
| F-06 | P2 | Quiet Recall Bubble contract drifted between a 2-to-3-item default and the current one-card-first product direction. | Default one visible card; stack only 2-3 independently passing distinct candidates. | T-11 Bubble DOM and stale-currentness tests. | Resolved |
| F-07 | P2 | Context action/long-press labels and presentation need i18n, touch, teardown, font/theme and reduced-motion evidence. | Minimal contract-restoring UI fixes only. | T-12 locale/DOM tests plus physical desktop typography/theme/reduced-motion, deterministic real-renderer context/Tab fixtures, light/dark forced-`:hover` evidence and real-device tap/target/Bubble evidence pass. CDP input proves the desktop renderer hold recognition path; synthetic events prove the iPhone renderer hold-menu/timeout/outside-dismiss/Capture path; direct production DOM tests and provider-free real Obsidian clicks prove Review/Discover callbacks/downstream presentation; the user reported desktop mouse and iPhone finger long-press passes. | Resolved |
| F-08 | P2 | A stale Pagelet detail leaf could remain `getMostRecentLeaf()` after returning to Markdown, preventing Pet remount after reload. | Prefer the focused workspace leaf for lifecycle mount/sync/toggle paths; keep source-navigation behavior unchanged. | Orchestrator regression proves focused Markdown wins over stale detail leaf. | Resolved |
| F-09 | P1 | Recap could repeat an unchanged successful snapshot after five minutes, and limiter wait/hot reload left narrow privacy or hard-cap races. | Reuse a current full-snapshot artifact; revalidate authorization/source/Data Boundary immediately before invoke; serialize shared persisted buckets across hot-reload instances. | Orchestrator, production adapter and two-instance limiter regressions. | Resolved |
| F-10 | P2 | Boundary filtering after the 12-note cap distorted disclosure/coverage; fallback omitted change/time facts; diagnostics disappeared on reload; context action targeted a nonexistent Bubble body. | Filter before bounded transfer, aggregate overflow honestly, render recent-source facts, persist a whitelisted content-free diagnostic shape, and mount context action on the Bubble root. | Production adapter, settings sanitization/reload, fallback DOM and Bubble view regressions. | Resolved |
| F-11 | P2 | Quiet Recall could invoke the provider after its limiter wait even if Pagelet/Recall was disabled, unload started or a source disappeared. | Recheck runtime toggles, unload state and the complete source identity immediately after reservation and again in the evaluator. | Deferred-limiter matrix proves zero invoke, zero cost and zero provider-call diagnostics for all four drift cases. | Resolved |
| F-12 | P2 | Pet unmount could leave a pending hold timer or visible menu listener alive across focused-leaf remounts. | Clear timer, menu, listener and touch-suppression state before the root early return. | Direct pending/visible-menu unmount regressions prove no later callback and complete listener/state cleanup. | Resolved |
| F-13 | P2 | Tracker claimed direct Recap nudge suppression and exact production feature caps without executing those integration/boundary paths. | Add background-ready nudge integration and actual Recap 2/h+10/day / Recall 10/h+50/day N/N+1 tests. | Orchestrator and production plugin limiter regressions pass. | Resolved |
| F-14 | P2 | Click-to-view Recap incorrectly reused the two-source proactive-hint gate, hiding a concrete single-source artifact. | Separate delivery from interruption: explicit click accepts one unambiguous source; proactive Recap still requires two distinct sources. | Parser, Recap builder, Bubble mapping and proactive-quality regressions cover both sides. | Resolved |
| F-15 | P1 | Local Discover candidates could inherit AI Recall presentation, and accepted AI candidates could re-enter the local fallback in mixed-ID runs. | Require `ai` provenance plus evaluation fingerprint for Recall, remove accepted IDs before constructing the local fallback, and render local clues as a minimal localized source list without AI fields or Recall stack. | Mixed accepted/rejected orchestrator and Bubble/Tab regressions pass; current-tree real Obsidian fixture shows one local clue, zero Recall cards and no sentinel AI fields. | Resolved |
| F-16 | P1 | Promotion of B-108 dropped the original dogfood residual scope from Backlog while decisions still linked a nonexistent Backlog row. | Route excluded double-Ctrl/Chat/frontmatter/Weekly Review/Pattern residuals to B-116; keep `replace_selection` under B-101/T-003; point decisions to the active B-108 package and deferred successor. B-116 avoids the permanently assigned B-115/GOV-001 identity. | Backlog, Decision traceability, Feature Home, governance ID audit and docs checker cover the durable route. | Resolved |
| F-17 | P1 | A bounded Scope Recap JSON request against supported DashScope hybrid models could inherit default thinking, spend the short budget before final content, and surface an empty answer as `malformed`; a broad Qwen3 family gate could also send the opt-out to fixed-mode models that reject it. | Preserve the 1000-token cap; explicitly disable thinking only for recognized DashScope hybrid families; exclude `thinking`/`instruct` variants and the known Qwen 3.7 thinking-only Max ids; classify blank final content as `empty` while keeping non-empty invalid JSON `malformed`; keep other providers/models unchanged and do not add an automatic retry. | Tri-state request-option/model-family and fixed-mode negative tests, Scope adapter assertion plus empty/malformed response regressions, 2-suite/234-test final focused gate, 192-test adapter suite, 159-suite/3154-test full gate, and the final successful real-provider artifact pass. | Resolved |
| F-18 | P2 | Adjust/Cancel persisted background preparation as disabled, but the currently open independent Settings window could keep showing the toggle as enabled until a reload. | Keep Adjust pending + disabled and Cancel declined + disabled; refresh only an already-visible PA Settings tab, using its owner document for lifecycle state so Cancel never opens Settings unexpectedly. | Orchestrator and independent-window Settings regressions, 3-suite/257-test focused gate, real Adjust/Cancel UI smoke, and independent re-review pass. | Resolved |
| F-19 | P2 | The formal 3-Second procedure omitted the authorized/prepared Recap precondition, so running it immediately after the Cancel smoke produced a CTA-only `intentionally-quiet` sample that could be mistaken for a Recap verdict. | Make authorization, preparation-on and fresh-artifact readiness explicit before the user click; classify the first sample as empty-state feedback, not prepared-Recap acceptance evidence. | Persisted setting audit plus coordinator/state-builder trace exactly explain the observed `Find related old notes`-only Bubble; handoff now requires the correct precondition. | Resolved |
| F-20 | P2 | After `Cancel`, re-enabling Scope Recap preparation in the same app session persisted pending/on but the session prompt guard could suppress the new disclosure, leaving no way to choose `Run`. | Track the observed authorization state and reset disclosure eligibility only for an explicit `declined-v1 → pending + enabled` transition; keep every provider path gated on a fresh `Run` choice. | Parameterized Run/Adjust/Cancel regression, 4-suite/323-test focused gate, 160-suite/3165-test local/iCloud deploy, independent review and the final real disclosure/Run path pass. | Resolved |

## Validation Log

| Date | Requirement / AC | Check | Result | Evidence / residual risk |
| --- | --- | --- | --- | --- |
| 2026-07-18 | Authority bootstrap | `npm run docs:check`; `git diff --check` | Passed | Final reconciled authority package: 226 Markdown files and 1389 local links checked; no whitespace errors. |
| 2026-07-19 | T-01 through T-12 | 15 focused Jest suites | Passed | 788/788 tests, including both Recap and Recall provider-boundary guards, complete snapshot, cross-instance limiter, Bubble/Pet/Tab, diagnostics reload, settings sanitization, locale, coverage and direct Recap section-to-card-style DOM regressions. |
| 2026-07-18 | All automated behavior | Full Jest via 2026-07-18 `make deploy` and `make deploy-icloud` gates | Passed | Both gates completed with 159/159 suites and 3128/3128 tests; lint, TypeScript and production build passed twice. |
| 2026-07-19 | Recap card-style evidence follow-up | Two focused suites, then full Jest | Passed | Direct payload mapping and rendered modifier-class assertions added; 2/2 suites with 146/146 tests and 159/159 suites with 3130/3130 tests passed. Runtime assets were unchanged at that point; current full/deploy evidence is recorded in the Scope Recap compatibility row below. |
| 2026-07-19 | Completion audit and pre-F-17 deploy | 15 focused suites; full Jest; lint; TypeScript; build; docs; whitespace/community scan; local/iCloud deploy and byte comparison | Passed | 807/807 focused tests and 159/159 suites with 3149/3149 full tests pass. Exact cap, nudge, single-source delivery, local/AI provenance and Pet lifecycle/callback regressions are included; docs check covers 226 Markdown files / 1398 local links, and both deployed runtime asset sets match `dist`. |
| 2026-07-18 | Local validation gate | TypeScript, ESLint, build, whitespace and community DOM scan | Passed | TypeScript, ESLint, build and `git diff --check` pass; community scan returned exit 1 with no matches, which is the expected pass result. |
| 2026-07-18 | Final local/mobile build preparation | `make deploy`; `make deploy-icloud`; byte comparison | Passed | Final assets copied to both local and iCloud `test` vault plugin directories; `main.js`, both manifests and `styles.css` match `dist` byte-for-byte. |
| 2026-07-18 | Recap authorization and ready path | Earlier deployed-build smoke + current-tree automated regressions | Passed | The earlier deterministic fake-provider smoke showed scope 10 included/2 skipped, qwen/deepseek-v4-flash, sanitized DashScope endpoint, 2/h+10/day cap, no source mutation and clearable local derived cache. One 3045-character fake prompt produced a fresh tension backed by `pagelet-smoke-golden.md` and `2026-06-29.md`; explicit open completed in 5 ms with fake calls unchanged. Current-tree regressions separately cover the subsequent F-08 through F-10 fixes. No external transfer or real quota. |
| 2026-07-18 | Recap failure/currentness/fallback | Earlier deployed-build smoke + current-tree automated regressions | Passed | The earlier deterministic failure/cache-clear smoke made one fake call, retained the current prior artifact and added zero calls on explicit open. Clear removed artifact/attempt state without rebuilding; fallback opened in 6 ms with 10/12 coverage, skipped count, Retry and View sources, and no inferred theme. Current-tree regressions separately cover the subsequent complete-snapshot, coverage, change/time and diagnostics fixes. |
| 2026-07-18 | Quiet Recall language/cooldown/cache | Earlier deployed-build smoke + current-tree automated regressions | Passed | The earlier deterministic candidate/provider smoke gave one high-related source an English initial result and one Chinese language retry; the retry was accepted with AI provenance and exact fingerprint. Diagnostics recorded 2 actual calls; immediate repeat returned one exact cache hit and 0 provider calls. Current-tree regressions separately cover the final limiter, diagnostics and lifecycle changes; local-only candidates remain explicit Discover-only `Local related clue` source lists without AI why-now, Recall stack or proactive state. |
| 2026-07-18 | Existing vault runner | Repo-local Pagelet smoke runner | Partial | 25 checks passed before unrelated pre-existing D6 `memory_migration_not_writable`; this is not evidence against B-108 but prevents claiming a whole-run pass. |
| 2026-07-19 | Desktop real-UI smoke | Obsidian 1.13.2 test vault + Computer Use + user physical gesture | Passed | After persisting `pagelet.enabled=true` and `petVisible=true` and reloading, real short-click opened/closed Bubble. Font 16px and 24px, light/dark theme, Panel/Detail/Bubble reflow, and macOS Reduce Motion on/off all rendered without observed overflow or overlap; original light/16px/motion-off state was restored. Renderer-level hover is completed in the separate row below; the user later reported the physical 520 ms mouse long-press menu passed. The subjective 3-Second Value Test was completed separately in the final validation row. |
| 2026-07-19 | Context-action and Tab typography visual fixtures | Obsidian 1.13.2 real renderer + deterministic in-memory payloads | Passed | Main Recap and localized context action were observed together with clear whitespace/type hierarchy and no overlap. Separate Tab fixtures covered h2, h4, body, tags, source links and a no-op action button at 16px and 24px, with readable wrapping and no overlap. These fixtures called the production Bubble/Tab renderers only: zero provider calls, zero note writes; the Detail leaf was closed, font restored to 16px and Obsidian reloaded. The first cleanup Force Reload transiently showed a blank shell; a second Force Reload restored the same test note, so no B-108 code finding was opened. |
| 2026-07-19 | Card hover light/dark contrast | Obsidian 1.13.2 real renderer + CDP `CSS.forcePseudoState` | Passed | A provider-free insight-card fixture was observed before/after real `:hover` CSS activation. Light mode changed `box-shadow` from `none` to `rgba(0,0,0,.12) 0 2px 8px` and border approximately 230→196; dark mode changed the same shadow from `none` and border approximately 54→83. Screenshots showed subtle but perceivable contrast with no content shift. This is renderer-level synthetic hover, not a claim of physical pointer movement. Authorization Run was never selected; zero provider calls and zero note writes. Forced state was cleared, Detail leaf detached, target note restored, theme/font/motion restored, debug detached and mobile emulation confirmed off; fresh error buffer was empty. |
| 2026-07-19 | Desktop hold-menu timing | Obsidian 1.13.2 real renderer + CDP `Input.dispatchMouseEvent` + user physical gesture | Passed | CDP emitted `pointerdown` / `mousedown` on the live 56×56 Pet; a temporary MutationObserver recorded Capture / Review / Discover insertion 523 ms after `mousedown`, matching the 520 ms contract. `mouseReleased` followed, no menu action, Bubble, provider call or note write occurred. That renderer evidence did not substitute for hand feel; the user later reported the physical 520 ms mouse long-press produced the same menu and passed. Temporary event listeners/observer were removed; menu/Bubble absent, fresh error buffer empty, debugger detached and mobile emulation off. |
| 2026-07-19 | iPhone 15 real-device smoke | Wireless iPhone Mirroring + Safari Web Inspector + user physical gesture | Passed | iCloud `main.js`, both manifests and `styles.css` match the current build. Real short-tap toggled Bubble; Inspector measured the mobile-toolbar Pet at 44×44. Synthetic TouchEvent on the real device renderer produced Capture/Review/Discover; the menu visibly auto-dismissed after 3 seconds. Synthetic hold plus outside pointer removed it, and Capture opened Quick Capture, which was cancelled without a write. The user later reported the physical iPhone long-press passed. Review/Discover live menu clicks were not inferred from that report and were not required for touch parity. Fresh Inspector log was cleared after the bounded smoke. |
| 2026-07-19 | User physical long-press reconciliation | Desktop mouse + iPhone finger | Passed | User report: desktop physical mouse long-press at the 520 ms contract produced the same Capture / Review / Discover menu; iPhone physical long-press passed. This closes the physical-gesture evidence gap without reclassifying prior synthetic events as physical input. |
| 2026-07-19 | Local Discover provenance UI | Obsidian 1.13.2 real renderer + deterministic in-memory local candidate | Passed | Production Tab rendered one localized `Local related clue` card and zero AI Recall cards. Sentinel title/summary/why-now/next-action fields were absent from DOM; screenshot and DOM inspection showed the source-list presentation without overlap. Zero provider calls and zero note writes; fixture/state were restored, Detail closed, target note reopened, fresh error buffer empty, debugger detached and mobile emulation off. |
| 2026-07-19 | Review/Discover downstream app routes | Obsidian 1.13.2 real renderer + Computer Use clicks + bounded provider-free seams | Passed | From the production Pet menu, a real Review click rendered the governed Bubble and then Current Note Analysis Panel; a real Discover click resolved `[[Pagelet Smoke Test]]` and rendered Connection Discovery with `Existing wikilink`. DOM assertions recorded Review=1, related lookup=1, Discover=1, `writeCalls=0`, `costDelta=0` and unchanged `.pagelet/` files. Screenshots showed both settled panels without overlap. Runtime seams/session state were restored, target note reopened, UI remnants absent, mobile emulation off and the fresh error buffer empty. This proves current-tree app routing/presentation, not real-provider semantic quality. |
| 2026-07-19 | Review/Discover real-provider semantics | Obsidian 1.13.2 + Computer Use + Qwen `deepseek-v4-flash` | Passed | Review sent `pagelet-provider-en.md` and returned three concrete, source-bound suggestions covering measurable acceptance criteria, rollback and go/no-go ownership; Bubble and Current Note Analysis Panel rendered correctly. Cost was 1608 input + 265 output = 1873 tokens. Discover sent four bounded test-vault sources (`2026-06-29.md`, `pagelet-smoke-golden.md`, `ux-journey/2026-06-29-quick-thoughts.md`, `2026-06-28.md`) and rendered three specific Chinese connections with the correct source list/graph. Cost was 1027 input + 521 output = 1548 tokens. No Save action was clicked; `.pagelet/` stayed empty, directly baselined sources stayed unchanged, fresh errors were empty, and the smoke/session state was cleared by plugin reload. |
| 2026-07-19 | Physical-evidence reconciliation | Persisted settings, asset `cmp`, `npm run docs:check`, `git diff --check` | Passed | Local/iCloud plugin settings both retain `pagelet.enabled=true` and `petVisible=true`; local Appearance is back at 16px/light and macOS `reduceMotion=0`. All four iCloud runtime assets still byte-match `dist`; the latest docs check reports 226 Markdown files / 1398 local links and whitespace check is clean. |
| 2026-07-18 | Runtime error buffer | `dev:errors` after restoration/reload | Passed with known-environment note | Two Memory/IndexedDB `onupgradeneeded` errors were induced by the direct settings-persistence probe and match the pre-existing D6 persistence problem; after full test-state restoration, plugin reload and buffer clear, no new errors were captured. |
| 2026-07-19 | Scope Recap structured-request compatibility | 4-suite compatibility gate; final 2-suite focused gate; 192-test adapter suite; TypeScript; whitespace; full local/iCloud deploy and byte comparison | Passed | `enableThinking` is tri-state and Scope Recap sends `enable_thinking=false` only for recognized DashScope hybrid families; explicit `thinking`/`instruct` variants plus `qwen3.7-max-preview` and `qwen3.7-max-2026-05-17` are excluded. Blank final content is `empty`; non-empty invalid JSON remains `malformed`; both keep one reserve/invoke and no retry. The earlier compatibility gate passed 348/348 tests, the final focused gate passed 234/234 and adapter suite 192/192, and final gates passed 159/159 suites with 3154/3154 tests; local/iCloud `main.js`, both manifests and `styles.css` match `dist` byte-for-byte. |
| 2026-07-19 | Optional Scope Recap real-provider token/cost | Obsidian 1.13.2 + Computer Use + Qwen/DashScope `deepseek-v4-flash` | Passed | The first foreground Retry made exactly one provider call and returned empty final content, historically recorded as 1014 input / 0 output and `malformed`; F-17 now classifies an identical future response as `empty`. No automatic retry, Markdown/source-note write, last-valid overwrite or background authorization followed. After the thinking-control fix, the second and final Retry succeeded at 1014 input + 574 output = 1588 estimated tokens, producing 1 theme, 1 tension, 1 open question and 12 sourceRefs with 12/22 coverage. The hourly limiter reached 2/2, background remained off, `.pagelet/` stayed empty and fresh console output had no errors. |
| 2026-07-19 | Authorization/settings completion evidence | Direct Modal/Retry regressions; Obsidian 1.13.2 Computer Use Adjust/Cancel; pre-F-20 local/iCloud gates | Passed | Five Modal tests cover disclosure copy, Run/Adjust/Cancel and direct close; provider-unconfigured Retry opens settings with zero provider/detail duplication/write. Real Adjust immediately rendered OFF with pending + disabled; real Cancel immediately rendered OFF with declined + disabled. Generic preparation stayed false, authorization context stayed null, high-value hint preference stayed true-but-inactive, and `lastAttemptAt` stayed `2026-07-19T08:37:26.579Z`; Run was never selected and recent-file audit found no `.pagelet` or Markdown note write. This pre-F-20 gate passed 257/257 focused tests and 160/160 suites with 3162/3162 full tests; the superseding reauthorization and 3165-test evidence is recorded below. |
| 2026-07-19 | 3-Second Value Test attempt 1 | User-authored fresh note + physical Pet click + persisted-state/runtime trace | Inconclusive for B-108; user-perception fail | User saw only `Find related old notes` and would perhaps click it because no substantive content was visible. Persisted state was `declined-v1`, Recap preparation off, generic hints off, Recall Bubble off and quiet state already acknowledged; this exactly selects an empty-body `intentionally-quiet` Bubble. The sample is retained as product feedback, but a user-authorized fresh prepared artifact must exist before the acceptance rerun. |
| 2026-07-19 | Same-session reauthorization regression | Orchestrator/settings/modal focused gate; TypeScript; full local/iCloud deploy; independent review | Passed | The exact `Cancel → Settings re-enable` sequence reopens disclosure once; Run invokes once, Adjust/Cancel invoke zero times. Four suites passed 323/323 tests; full local/iCloud deploy each passed 160/160 suites and 3165/3165 tests with lint/build; all four assets in both destinations byte-match `dist`; independent review found no P0-P2 issue. |
| 2026-07-19 | 3-Second Value Test attempt 2 | User-authorized prepared Recap + persisted diagnostics + Pet ready state + physical click | Passed | Before click, authorization was `authorized-v1`, preparation on, a fresh 12-source background attempt succeeded at 995 input + 639 output tokens, and Pet exposed `insights ready`; after click the same attempt timestamp/snapshot/cost remained unchanged, proving zero wait and zero duplicate foreground call. User saw a meaningful Recap whose honest recognition of the test vault's limited real-world value increased trust, and said they would open it again to see future discoveries and deeper understanding. |
| 2026-07-19 | BRAT prerelease publication | `make release-dry-run VERSION=2.9.0-beta.2`; `make release`; `make publish`; GitHub Actions run `29685255023` | Passed | Full release gate passed with 160 suites / 3165 tests, docs/lint/build/bundle audit and signed release metadata. GitHub Release `2.9.0-beta.2` is a prerelease with LICENSE, main.js, manifest.json, NOTICE, styles.css and THIRD_PARTY_NOTICES.md; beta release commit/tag remain isolated on `beta/2.9.0-beta.2`. Stable release was not created. |
| 2026-07-19 | Desktop BRAT smoke | BRAT 2.2.0 fixed-version install from GitHub prerelease; plugin reload; Settings/Chat/Pagelet | Passed | BRAT persisted `edonyzpc/personal-assistant` at `2.9.0-beta.2`; runtime manifest loaded beta.2. Personal Assistant Settings and independent Recap controls opened, Chat mounted with an empty composer and no provider call, and Pagelet displayed/opened the prepared source-backed Recap without a duplicate call. `dev:errors` reported no captured errors. Release `main.js`/`styles.css` byte-match the installed files; BRAT's compact manifest is semantically identical to the release asset. |
| 2026-07-19 | iPhone BRAT smoke | iPhone 15 Mirroring + Safari Web Inspector + BRAT 2.2.0 fixed-version install | Passed | BRAT was installed/enabled in the iCloud `test` vault, selected `2.9.0-beta.2 (Prerelease)`, registered the repository and persisted the frozen version. After plugin reload the real WKWebView runtime reported `2.9.0-beta.2`; Inspector measured the Pet at 44×44 and Mirroring short taps closed/reopened the visible `Find related old notes` Bubble. Release `main.js`/`styles.css` byte-match the iCloud files and manifest semantics match; a cleared fresh Console reported no errors. Earlier user-operated physical iPhone long-press remains valid because the published runtime/CSS bytes are identical. |

## Decision Log

| Date | Decision | Impact |
| --- | --- | --- |
| 2026-07-18 | [DEC-017](../../../product/decisions/dec-017-default-background-recap-preparation.md) | Recap prepares only after affirmative disclosure, with bounded independent cost and durable opt-out. |
| 2026-07-18 | [DEC-018](../../../product/decisions/dec-018-quality-gated-scope-recap-hints.md) | Only fresh, concrete, source-backed high-value Recap may lightly nudge. |
| 2026-07-18 | [DEC-019](../../../product/decisions/dec-019-honest-layered-recap-fallback.md) | Failure keeps valid content or shows honest local orientation; no fake insight. |
| 2026-07-18 | [DEC-020](../../../product/decisions/dec-020-independent-quiet-recall-evaluation.md) | Recall candidates are judged independently; actual provider calls are bounded and templates cannot substitute. |

## Closeout Readiness

- [x] Owning Product Spec 与实际行为一致。
- [x] Supporting Quiet Recall/Bubble contracts与实际行为一致，且没有第二套 B-108 IDs。
- [x] Architecture/current Pagelet design 与实际行为一致。
- [x] 所有 B-108 requirement/AC 都有 focused test、review 与必要 smoke evidence。
- [x] `make deploy` 和 repo-local Obsidian 三秒价值 smoke 已通过；移动端 claim 有真机证据或明确 residual risk。
- [x] 所有 P0/P1/P2 findings 已关闭或由用户明确延期。
- [x] 未完成项已进入 Backlog。
- [x] `closeout.md` 已记录每份 artifact/handoff/validation evidence 的信息 disposition。
- [x] Active Registry 与 `docs/archive/2026/pagelet-b108-dogfood-followup/` 更新方案明确。
