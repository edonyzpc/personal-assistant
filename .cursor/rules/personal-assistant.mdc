---
description: 
globs: 
alwaysApply: true
---
## Personal Assistant 项目 Cursor Rules

### 项目说明
Personal Assistant 是一个运行在 Obsidian 内的插件，基于 [Obsidian 插件开发规范](https://docs.obsidian.md/Plugins/Getting+started/Build+a+plugin) 开发。当前技术栈以 TypeScript、React 19、Radix UI、Tailwind CSS 和 esbuild 为主。

### 1. 依赖与环境管理
- **依赖管理统一使用 `npm`**，开发环境基线为 `Node 22 LTS + npm 10.x/11.x`。
- 常用命令：
  - 安装依赖：`npm install`
  - 开发构建：`npm run dev`
  - Tailwind 监听：`npm run dev:tailwind`
  - 生产构建：`npm run build`
  - 代码检查：`npm run lint`
  - 测试：`npm test`
  - 发版：`make release` 或 `make release VERSION=1.6.0`

### 2. 代码结构说明
- `./docs`，文档目录
- `./test`，测试目录
- `./src`，源码目录
```
./src
|-- ai-services                 # AI provider、prompt、vector store 相关实现
|-- components                  # React 组件，例如 RecordList、Statistics
|-- stats                       # 统计逻辑与编辑器统计插件
|-- obsidian-hack               # Obsidian 移动端调试功能实现
|   `-- obsidian-mobile-debug.ts
|-- main.ts                     # 插件入口
|-- plugin.ts                   # Obsidian 插件主体实现
|-- preview.ts                  # RecordList 视图入口
|-- stats-view.ts               # Statistics 视图入口
|-- chat-view.ts                # 聊天侧边栏入口
`-- vss.ts                      # 文本向量检索与缓存实现
```
